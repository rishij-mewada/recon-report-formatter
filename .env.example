# Recon Document Generator Configuration

# API Server Port
API_PORT=8000

# Base URL for download links (set to your VPS URL)
BASE_URL=http://your-vps-ip:8000

# Optional API key for authentication (leave empty to disable)
API_KEY=

# Output directory (inside container)
OUTPUT_DIR=/app/output
